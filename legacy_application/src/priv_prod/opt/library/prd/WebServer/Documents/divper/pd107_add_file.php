<?php$database="divper";include("../../include/iConnect.inc"); // database connection parameters// extract($_REQUEST);//print_r($_FILES); //echo "<pre>"; print_r($_REQUEST); print_r($_FILES);echo "</pre>";  exit;//exit;if ($submit == "Add File") {extract($_FILES);$size = $_FILES['file_upload']['size'];if($size>"500000"){echo "The file you attempted to upload is larger than 500kb. You will need to rescan the PDF at a size less than 500kb.<br /><br />Click your browser's back button to return to the upload page.";exit;}$type = $_FILES['file_upload']['type']; if($type!="application/pdf"){echo "You must upload the PD107 as a PDF file. Please click your browser's back button and ";exit;}$var = explode("/",$type);$ext=$var[1];//print_r($_FILES); print_r($_REQUEST);exit;//echo "<pre>";print_r($_FILES); print_r($_REQUEST);echo "</pre>"; exit;if(!is_uploaded_file($file_upload['tmp_name'])){print_r($_FILES);  print_r($_REQUEST);exit;}/*$uploaddir = "file_upload/"; // make sure www has r/w permissions on this folder		$file=str_replace("/","_",$file);		$file=str_replace(",","_",$file);		$file=str_replace(" ","_",$file);		$file=str_replace("'","_",$file);		$file=str_replace("#","_",$file);*/$tempID=$last_name.$ssn;$file=$tempID."_".$beacon_num."_pd107.pdf";$newdate=date("Y-m-d");    $dir = explode("-",$newdate);    $dirname = $dir[0];    $folder = "pd107s/".$dirname;if (!file_exists($folder)) {mkdir ($folder, 0777);}    $dirname = $dir[0]."/".$dir[1];    $folder = "pd107s/".$dirname;if (!file_exists($folder)) {mkdir ($folder, 0777);}    $uploadfile = $folder."/".$file;//echo "$uploadfile";exit;move_uploaded_file($file_upload['tmp_name'],$uploadfile);// create file on server$sql = "REPLACE pd107_uploads set form_name='$form_name', tempID='$tempID', beacon_num='$beacon_num', file_link='$uploadfile'";//  echo "$sql";exit;$result = @mysqli_query($connection,$sql) or die(" $sql Error 1#". mysqli_errno($connection) . ": " . mysqli_error($connection));    mysqli_CLOSE($connection);    	header("Location: pd107_upload.php?beacon_num=$beacon_num");exit;	}?>