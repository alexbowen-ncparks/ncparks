<?php$database="budget";$db="budget";include("/opt/library/prd/WebServer/include/iConnect.inc"); // connection parametersmysqli_select_db($connection, $database); // databaseinclude("../../../include/authBUDGET.inc");//echo "<pre>";print_r($_REQUEST);//print_r($_SESSION);//echo "</pre>";//exit;//echo "<pre>";print_r($transfer_request);echo "</pre>";if(round(array_sum($transfer_request),2)!=0){$t=round(array_sum($transfer_request),2); echo "Transfer amounts do not equal zero. They equal $t  Click your browser's back button and make sure any request for additional funds in an account is offset by a reduction elsewhere.";exit;} foreach ($transfer_request as $key => $value) {	$value=str_replace(",","",$value);// remove any commas 1,000 => 1000 $sql="REPLACE opexpense_transfers_4 SET f_year='$_REQUEST[f_year]', transfer_date='$_REQUEST[today]', center='$_REQUEST[center]', ncas_number='$key',transfer_request='$value', user_id='$user_id', source='$source'";	//	echo "$sql<br>";	$result = mysqli_query($connection, $sql) or die ("Couldn't execute query 1. $sql");}// end for//echo "<br><br>The ability to edit is still be developed. We need to resolve some field name differences.<br><br>Click your browser's BACK button.";//exit;extract($_REQUEST); header("Location: /budget/a/op_exp_transfer.php?center=$center&f_year=$f_year&submit=Submit");?>